/* VARIABLE ADMINISTRADOR -------------------------------------------------------------------- */
let userName="admin";
let adminLicence=false;
if (userName === "admin") {
    adminLicence=true
}
let userState= false;

export {adminLicence, userName, userState};